---
# Main playbook - just test connectivity at this stage

- hosts: all
  name: Test connection to all servers
  become: true
  gather_facts: true
  
  tasks:
    - name: Ping all servers
      ping:
    
    - name: Print server information
      debug:
        msg: "Successfully connected to {{ inventory_hostname }} ({{ ansible_distribution }})"